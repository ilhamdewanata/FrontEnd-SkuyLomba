import React from "react";

const Subtitle = ({ text }) => {
  return (
    <h2 className="subjudul text-3xl font-bold text-[#0098FF] text-center">
      {text}
    </h2>
  );
};

export default Subtitle;
